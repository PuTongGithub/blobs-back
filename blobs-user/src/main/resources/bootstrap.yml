spring:
  cloud:
    config:
      name: user
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: blobs-config
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka/